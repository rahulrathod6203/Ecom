spring:
  application:
    name: product-service

  datasource:
    url: jdbc:postgresql://localhost:5432/ecomdb
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: update
  server:
    port: 9090
